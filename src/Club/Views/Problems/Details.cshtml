@using System.Threading.Tasks
@using Club.ViewModels
@model Club.ViewModels.ProblemViewModel

@{
    ViewBag.Title = Model.Name;
}


<div class="row">
    <div class="col-md-12 ">
        <div class="jumbotron">
            <h1>@Model.Name</h1>
            <h3>@Model.Topic — @Model.Level</h3>
            <input type="hidden" data-start="0" data-stop="10" class="rating" value="@Model.Difficulty" data-readonly />
            <h4><a href="@Model.Link">@Model.Site</a></h4>
        </div>
    </div>
</div>



@section Scripts{
    <script src="~/lib/bootstrap-rating/bootstrap-rating.min.js"></script>
}